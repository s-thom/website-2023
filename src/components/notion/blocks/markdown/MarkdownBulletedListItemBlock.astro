---
import type { BulletedListItemBlockObjectResponse } from "@notionhq/client/build/src/api-endpoints";
import { richTextToUnformattedString } from "../../../../lib/notion/util";

export interface Props {
  block: BulletedListItemBlockObjectResponse;
}

const { block } = Astro.props;
---

<Fragment>
  {`- ${richTextToUnformattedString(block.bulleted_list_item.rich_text)}`}
  <slot />
</Fragment>
