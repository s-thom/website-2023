---
import type { Heading1BlockObjectResponse } from "@notionhq/client/build/src/api-endpoints";
import { richTextToMarkdownString } from "../../../../lib/notion/util";

export interface Props {
  block: Heading1BlockObjectResponse;
}

const { block } = Astro.props;

const plain = richTextToMarkdownString(block.heading_1.rich_text);
---

<Fragment>
  {`## ${plain}`}
  {"\r\n"}
</Fragment>
