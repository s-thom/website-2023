---
import type { ToggleBlockObjectResponse } from "@notionhq/client/build/src/api-endpoints";
import { richTextToUnformattedString } from "../../../../lib/notion/util";

export interface Props {
  block: ToggleBlockObjectResponse;
}

const { block } = Astro.props;
---

<Fragment>
  {
    `<details>\r\n<summary>${richTextToUnformattedString(block.toggle.rich_text)}</summary>\r\n`
  }
  <slot />
  {`</details>\r\n\r\n`}
</Fragment>
