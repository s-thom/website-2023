---
import type { RichTextPropertyItemObjectResponse } from "@notionhq/client/build/src/api-endpoints";
import RichText from "../common/RichText.astro";

export interface Props {
  property: RichTextPropertyItemObjectResponse;
}

const { property } = Astro.props;
---

<RichText components={[property.rich_text].flatMap((r) => r)} />
