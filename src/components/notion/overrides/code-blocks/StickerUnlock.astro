---
import type { StickerTypes } from "../../../../stickers/types";
import { LazyLoader } from "../../../react/LazyLoader.tsx";

export interface Props {
  type: StickerTypes;
  hideOnUnlock?: boolean;
}

const { type, hideOnUnlock } = Astro.props;
---

<LazyLoader type="sticker-unlock" props={{ type, hideOnUnlock }} client:idle />
